{{> layout/header}}

<section>
    <!--/delete 적는 이유. 연습을 위해 http 1.0 방식으로 만들어서
    post로 put, delete, insert를 다 구현했기 때문에 구분을 위해서-->
    <form action="/board/{{model.id}}/delete" method="post">
        <button type="submit">삭제</button>
<!--        body가 필요없다. id 정보는 패스변수(url)에 있다. -->
    </form>
    <form action="/board/{{model.id}}/update" method="get">
        <button type="submit">수정</button>
    </form>
    <div>
        번호 : {{model.id}} <br>
        제목 : {{model.title}} <br>
        내용 : {{model.content}} <br>
        작성일 : {{model.createdAt}} <br>
    </div>
</section>

</body>
</html>